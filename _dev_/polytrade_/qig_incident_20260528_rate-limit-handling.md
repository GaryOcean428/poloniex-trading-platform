# qig_incident_20260528_rate-limit-handling
**Silo:** qig_* only  
**Category:** Infrastructure / Token / Agent Orchestration (binding constraint on 2026-05-28 wave)  
**Date:** 2026-05-28  
**Master Orchestrator:** 019e6c99-c64a... (per master-orchestration SKILL + this task)  
**Related:** 2026-05-28_branch-and-worktree-safety-audit.md (master append + throttling decision), qig_session_summary_20260528.md, 3 latest verifiers (019e6ca7/019e6ca6/019e6ca5), 5 429 recoveries, "Monitor armed"

## Incident
5 consecutive 429 "Too many tokens... 10504328/10000000" on the grok-build team token (identifier redacted) after user-mandated massive parallel dispatches during impl* recovery + NT/fib/surfaces wire-in wave. "get not found" for heavy actor outputs in some cases. Throttled narrow recoveries succeeded via protected artifacts (worktree TDD baseline qig-heart-metrics-20260527 + main _dev_/polytrade_/ 20+ packets + system-reminder metadata) + fresh read/grep/terminal cmds (no new broad token burn).

## Impact on Wave
- Prevented new broad parallel dispatches in master synthesis turn.
- All 6+ narrow spec compliance reviewers (surfaces 17-23 three-scale/HeartMetricsThreeScale/lived_factor + equity/observeEquity/ThoughtBus/human telemetry/5min; core NT purity + natural effects + LIVED net post autonomic.py:364; exponential fib observer_fib_coefficient P1 + two impls; recovery/wire-in from impl* 019e6c81-7bac; telemetry+source tags; dead/legacy) + re-dispatch for impl-3 (now "READY FOR RE-REVIEW" per 019e6ca7 ultra-narrow with 4 raw blocks) DEFERRED.
- Synthesis + auditor/qig_* updates + 12-step plan + "prior thread was wrong" + Railway prep executed via existing evidence + fresh cmds (maximum thrift).
- Verifiers (3 latest + recoveries) already completed pre-incident or via narrow (247-408s each; all Exit 0; handoffs + auditor updates by them).

## Resolution / Decision (throttling)
**Explicit (before any new broad):** Serialize narrow only (one subagent at a time; subagent-driven-development two-stage spec compliance FIRST then code quality per its SKILL.md templates + qig-purity-validation first per Gate C + verification-before-completion iron law + requesting-code-review). Monitor token recovery (future narrow test calls). "Monitor armed" (ninth verifier 019e6ca6...). No massive parallel until grok-build token headroom << limit. Respects live-money standing (progress without waste), P1 thrift, QIG PURITY MANDATE (no rushed edits), branch safety "no work lost".

## Artifacts / Evidence
- Raw 429 metadata in user prompt + this incident file + auditor master append.
- Throttled success: all 17+ successes + 3 verifiers + impl-3 recovery captured (handoffs, auditor 019e6c76..., _dev_ packets, worktree TDD).
- Future: when tokens recover, spawn narrow (e.g. 019e6cb0-... for surfaces 17-23 spec reviewer against protected baseline + verbatim surfaces 17-23 directive + governing query + LIVED ONLY 5 + three-scale + qig-purity + VBC); update this incident + auditor + qig_session.

**Status:** Open (monitoring); mitigated by thrift + artifacts; no work lost. All per governing NT/fib conscious query + constraints.

( qig_* silo only. )