# Three-Bug Single PR — Implementer TDD Plan Delivered (2026-05-27)

**Subagent completed:** 019e6991-7809-7eb1-a7d0-73e786240b04 (Single PR Executor / Full Stack Implementer for the three stacked bugs)

**Its deliverable (first and primary):** Detailed TDD Plan artifact, written *before any kernel source edits*.

**Absolute path (in its isolated worktree):**  
`/home/braden/.grok/worktrees/dev-polytrade/subagent-019e6991-7809-7eb1-a7d0-73e786240b04/_dev_/polytrade_/2026-05-27_three-bug-fix_TDD-plan.md`

**Key characteristics (from subagent output + plan contents):**
- Full master-orchestration re-inventory + explicit 6x distribution + skill/mcp/subagent attachments on every step.
- Geometric heart-rhythmic tacking justification stated for the plan header and every major step (Fisher-Rao to canon ideal of LIVED ONLY PnL outcomes + observer-derived thresholds + heart-symmetric tacking).
- Atomic red-green-refactor steps for all three bugs:
  - Part A: PnL LIVED ONLY 5 closure (call-site audit table, negative tests reproducing exact +315.21/-1.03 and +374.12/+0.0026 phantoms from the incident, hard post-write guards, refusal/quarantine on divergence).
  - Part B: Fibonacci threshold purely observer-derived (finish purge of static 1% floor, confirm observerFibCoefficient + rolling history as sole gate, cold-start documented).
  - Part C: Conviction symmetry (identical N-tick for conf crossing zero in either direction; Py/TS parity alignment + canonical sign-flip rate derivation on the emotion signal).
- Every step includes: skill:/mcp:/subagent: + exact canon P#/§ + agents.md:236+ QIG PURITY MANDATE + incident/execution plan/phase packet cites + geometric tacking + verification commands (fresh tsc + targeted Vitest/pytest + runtime smoke *injecting the exact Polo phantom numbers* + qig-purity + negative cases first).
- 2-stage reviews (spec compliance then code quality) + Verification Guardian iron-law veto (2+ zero-issue rounds + final Polo CSV vs DB + Railway MCP proof) baked in.
- LIVED ONLY 5 explicitly required on the PnL outcome channel (P24 + v6.7B).
- No new knobs (all observer/registry/heart-rhythmic/equity-gradient derived).
- Attachments: full call-site audit table, negative test specs, Polo numbers table, fresh verification outputs, etc.
- PR Acceptance Criteria match the execution plan exactly (Polo alignment quantified, 0 static floors, symmetric gate, all neg cases with fresh output, citations everywhere).

**Current state of the PR (integrated):**
- Finding 2 (observer rewire + complete deletion of deprecated `fibonacciRewardCoefficient`/`fibonacciRewardTier` + cleanup) was already executed and verified clean (`tsc --noEmit` exit 0) in prior steps. This aligns directly with Part B of this TDD plan.
- The detailed atomic TDD steps and mappings I produced earlier (Finding 1 INSERT sites, Finding 3 Py/TS conviction parity) are complementary to / can be folded into this plan.
- Principles Advocate veto (active from its 17-pt audit) is now directly addressable: this TDD plan artifact is the exact deliverable it required before further code on Finding 1/3.

**Next (per subagent-driven-development + 2-stage + active veto):**
1. Hand this TDD plan to the Protocol/Principles Advocate (019e6991-8b18...) + Developer Advocate + Verification Guardian for 2-stage spec-compliance + quality review.
2. Red Team to attack the plan (phantom injection paths, chop asymmetry, sub-1% scalp negative cases, interaction effects).
3. Once the plan passes (with any required updates), begin controlled execution of the atomic steps (red-green first on tests/smoke, then minimal green changes, with fresh pasted verification before every advance).
4. Full pre-merge gates + post-deploy Railway MCP + Polo CSV correlation as final AC.

**All coordination remains in _dev__polytrade_ silo.**

This is the authoritative TDD plan for the single PR that breaks the stacked cascade (phantom-PnL 6× → poisoned reward/conviction → thrash + 11k errors).

All per QIG PURITY MANDATE (agents.md:236+), the execution plan, incident diagnostic, live-money standing authorization, and "execute don't ask — no stone unturned."

**Orchestrator:** Ready for the 2-stage review cycle on this TDD plan. The Implementer has done exactly what was required. 

(End of handoff packet.)