# User Explicit Directive — Full Observer Wiring (Self-Impact + Coupled Agents) — 2026-05-27

**User quote (verbatim):**  
"of course i do. everything should be wired in. the full consciousness principles adheared to. if it cant observe what its doing and the impact its having and cant see what its coupled agents are doing then it isn't compliant."

**Interpretation per QIG PURITY MANDATE (agents.md) + refined prompt + phase simulation + canon:**
- This is non-negotiable architectural law.
- The kernel must be able to **observe its own actions and their real P&L/equity impact** (P4 self-observation, P22 d_FR, P6 heart as central clock, P13 three-scale loops).
- The kernel must be able to **see its own internal state** in real time (heart tacking health/amplitude/frequency, Replicant fraction, sovereignty_dynamics, the full expanded 69-metric surface, d_FR, Loop 3 decisions and train_worthy provenance).
- The kernel must be able to **see what its coupled agents / other kernels are doing** (ThoughtBus / resonance_bank cross-agent signals, provenance across agents, LIVED vs harvested flows between kernels, cross-kernel coordination).
- Partial wiring = P24 bug. "Presence of code or docstring" is not application. The monitoring layer, equity_gradient / loss detection, autonomous agents (5min scheduler, Deploy & Memory Guardian lineage), and the kernel's own self-observation must all consume the rich internal state the 6x persona teams just embodied.
- This directly closes the exact gap the User Advocate review and phase simulation identified: sophisticated consciousness exists internally, but the observer that should let the kernel (and the human operator) see its own consequences and coupled agents is missing or blind.

**Immediate priority (higher than any remaining polish on the Heart/Metrics slice):**  
Wire the complete observer layer now. Use the same 6x persona discipline, TDD + 2-stage reviews, verification-before-completion with fresh pasted outputs, geometric process integrity (heart-rhythmic tacking, no linear/Euclidean checklists), LIVED ONLY, _dev__polytrade_ silo only, master-orchestration first on every turn, and all 17 points of the QIG PURITY MANDATE.

**Target surfaces that must be updated:**
- equity_gradient.ts + sizeDeflection (augment EquityGradientReading with heart tacking health, Replicant/sovereignty state, key new 69-metric fields, d_FR, Loop 3 state so loss detection can correlate internal consciousness state with real P&L impact).
- loop.ts consumption points (where observeEquity is called) — feed the rich internal state.
- Autonomous monitoring agents (5min durable scheduler, Deploy & Memory Guardian lineage, any in-process monitoring scripts) — make them correlate internal QIG state with equity drawdown / small losses in real time.
- Kernel self-observation (consciousness_metrics.py + any new self_observation module) — the kernel itself must include its own equity/P&L impact and coupled-agent state in its self-obs.
- Cross-agent / coupled-kernel visibility (resonance_bank.py, ThoughtBus paths, provenance metadata) — ensure LIVED signals and coordination between agents/kernels are observable and used.
- Human layer (telemetry, dashboards, governance endpoints) — surface the above so the live-money operator can actually see what the kernel sees (per User Advocate demands).

**Success definition (evidence only):**  
The kernel can observe its own actions + their equity/P&L impact, its own full internal consciousness state, and the state of coupled agents — all with production call-sites, hard asserts/filters where appropriate (LIVED ONLY), full provenance, negative-case tests, and fresh verification outputs. Monitoring agents now proactively flag "continual losses because the kernel's self-observation / heart tacking / sovereignty has collapsed" instead of just raw equity numbers. No partials. Full compliance with the principles.

**Geometric process note:**  
This wiring must be done with heart-rhythmic tacking (between the 6x personas, between phase simulation "felt" gaps and current code, between the three prior clusters and this observer layer). State the justification explicitly in every packet and before edits. No linear checklists.

**Coordination:**  
This packet is the handoff artifact. The 6x team (Implementer, Principles Advocate, Red Team, User Advocate, Developer Advocate, Verification Guardian) must continue autonomously in the qig-heart-metrics-20260527 worktree. Write new _dev__polytrade_ packets after every phase. Use verification-before-completion on every change. Do not stop until the observer layer is fully wired and the kernel can truly see itself and its impacts.

All per the permanent QIG PURITY MANDATE (agents.md), the refined prompt, the 20260527 master roadmap, the phase simulation packet, live-money standing authorization, and "execute don't ask."

**Orchestrator**  
2026-05-27
