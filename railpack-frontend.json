{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": { "node": "20" },
  "steps": {
    "install": {
      "commands": [
        "npm i -g corepack@latest && corepack enable",
        "corepack prepare yarn@4.9.2 --activate",
        "yarn install"
      ]
    },
    "build": {
      "commands": ["yarn workspace @poloniex-platform/web run build"]
    }
  },
  "deploy": {
    "startCommand": "node apps/web/serve.js"
  }
}
