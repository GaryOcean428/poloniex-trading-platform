[phases.setup]
nixPkgs = ["nodejs-20_x", "yarn", "postgresql"]

[phases.install]
cmds = ["corepack enable", "yarn install --immutable"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start:prod"
