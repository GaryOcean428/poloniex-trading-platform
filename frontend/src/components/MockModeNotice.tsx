import React from 'react';

/**
 * MockModeNotice - Disabled
 * This component has been disabled as the platform now uses real data exclusively.
 * Keeping the component for backwards compatibility but it renders nothing.
 */
const MockModeNotice: React.FC = () => {
  // Always return null - no mock mode notice needed
  return null;
};

export default MockModeNotice;
