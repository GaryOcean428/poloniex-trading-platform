{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20",
    "yarn": "4.9.2"
  },
  "steps": {
    "install": {
      "commands": [
        "npm i -g corepack@latest",
        "corepack enable",
        "corepack prepare yarn@4.9.2 --activate",
        "yarn install --immutable --immutable-cache"
      ]
    },
    "build": {
      "commands": [
        "yarn workspace frontend run prebuild",
        "yarn workspace frontend run vite build",
        "yarn workspace frontend exec rm -rf .shared-build"
      ],
      "inputs": [{"step": "install"}]
    }
  },
  "deploy": {
    "startCommand": "yarn workspace frontend run start",
    "inputs": [{"step": "build"}]
  }
}
