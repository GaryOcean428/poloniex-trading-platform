# Frontend Environment Variables (Railway-ready)

NODE_ENV=production

# Backend API Configuration (Required for autonomous trading)
VITE_BACKEND_URL=https://polytrade-be.up.railway.app
VITE_API_URL=https://polytrade-be.up.railway.app

# WebSocket Configuration (optional - will be derived from VITE_API_URL if not set)
# VITE_WS_URL=wss://polytrade-be.up.railway.app

# Railway Environment Variables (for Railway deployment)
# These are automatically set by Railway, no need to configure manually
# VITE_RAILWAY_PUBLIC_DOMAIN=${RAILWAY_PUBLIC_DOMAIN}
# VITE_RAILWAY_PRIVATE_DOMAIN=${RAILWAY_PRIVATE_DOMAIN}

# Poloniex API Configuration (per official API docs)
# REST API: https://api.poloniex.com (both Futures and Spot)
VITE_POLONIEX_API_BASE_URL=https://api.poloniex.com

# WebSocket URLs - Futures V3 API
VITE_POLONIEX_FUTURES_WS_PUBLIC=wss://ws.poloniex.com/ws/v3/public
VITE_POLONIEX_FUTURES_WS_PRIVATE=wss://ws.poloniex.com/ws/v3/private

# WebSocket URLs - Spot API
VITE_POLONIEX_SPOT_WS_PUBLIC=wss://ws.poloniex.com/ws/public
VITE_POLONIEX_SPOT_WS_PRIVATE=wss://ws.poloniex.com/ws/private

# Poloniex API Credentials (Required for live trading)
# WARNING: Only use VITE_ prefixed variables for frontend - these are public!
VITE_POLONIEX_API_KEY=your_poloniex_api_key_here
VITE_POLONIEX_API_SECRET=your_poloniex_api_secret_here
VITE_POLONIEX_PASSPHRASE=your_poloniex_passphrase_here

# Mock Mode Configuration
VITE_FORCE_MOCK_MODE=false
VITE_DISABLE_MOCK_MODE=false

# DO NOT USE THESE IN FRONTEND (No VITE_ prefix = backend only):
# JWT_SECRET - Backend only, never expose to frontend
# DATABASE_URL - Backend only, never expose to frontend  
# API_ENCRYPTION_KEY - Backend only, never expose to frontend
